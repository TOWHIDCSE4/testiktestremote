{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [
        "NODE_ENV",
        "ENCRYPT_KEY",
        "SIGN_KEY",
        "MONGO_URL",
        "REDIS_URL",
        "ORIGINS",
        "PORT",
        "API_URL",
        "WEB_URL",
        "MEDIA_URL",
        "MEDIA_KEY",
        "SENTRY_WEB_DSN"
      ]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
